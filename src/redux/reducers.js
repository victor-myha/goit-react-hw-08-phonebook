// User Reducer
const userInitialState = [];

export const userReducer = (state = tasksInitialState, action) => {
  // Reducer code
};

// Contacts Reducer
const contactsInitialState = [];

export const contactsReducer = (state = contactsInitialState, action) => {
  // Reducer code
};